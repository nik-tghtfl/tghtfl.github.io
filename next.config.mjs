/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  basePath: '/tghtfl.github.io',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
